<mah:MetroWindow x:Class="ThatSkyAppV2.UI.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="{DynamicResource Str.Settings.Title}"
        Height="500"
        Width="550"
        SizeToContent="Height"
		WindowStartupLocation="CenterOwner"
		ResizeMode="NoResize"
		WindowTitleBrush="#FF1A2026"
		TitleCharacterCasing="Normal"
		TitleAlignment="Center"
		TitleBarHeight="30"
		Background="#FF111111"
		NonActiveBorderBrush="#FF1A2024"
		BorderThickness="3"
		BorderBrush="#FF1A2026"
		NonActiveWindowTitleBrush="#FF1A2026">

	<mah:MetroWindow.Resources>
		<!-- TextBox Style -->
		<Style x:Key="SettingsTextBoxStyle" TargetType="TextBox">
			<Setter Property="Background">
				<Setter.Value>
					<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
						<GradientStop Color="#FF111111"/>
						<GradientStop Color="#FF1A2026" Offset="1"/>
					</LinearGradientBrush>
				</Setter.Value>
		</Setter>
		<Setter Property="Foreground" Value="White"/>
		<Setter Property="BorderThickness" Value="1"/>
		<Setter Property="BorderBrush" Value="#FF20A1C3"/>
		<Setter Property="Padding" Value="8,5"/>
		<Setter Property="VerticalContentAlignment" Value="Center"/>
		<Style.Triggers>
			<Trigger Property="IsMouseOver" Value="True">
				<Setter Property="BorderBrush" Value="#FF25B6DE"/>
			</Trigger>
			<Trigger Property="IsFocused" Value="True">
				<Setter Property="BorderBrush" Value="#FF25B6DE"/>
			</Trigger>
		</Style.Triggers>
	</Style>

		<!-- Combobox Style -->
		<Style x:Key="SettingsComboBoxStyle" TargetType="ComboBox">
			<Setter Property="Background">
				<Setter.Value>
					<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
						<GradientStop Color="#FF111111"/>
						<GradientStop Color="#FF1A2026" Offset="1"/>
					</LinearGradientBrush>
				</Setter.Value>
			</Setter>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="Height" Value="26"/>
			<Setter Property="Padding" Value="5,3,5,3"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="BorderBrush" Value="#FF20A1C3"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="ComboBox">
						<Grid>
							<ToggleButton x:Name="ToggleButton"
										  IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
										  Background="{TemplateBinding Background}"
										  BorderBrush="{TemplateBinding BorderBrush}"
										  BorderThickness="{TemplateBinding BorderThickness}">
								<ToggleButton.Template>
									<ControlTemplate TargetType="ToggleButton">
										<Border x:Name="ToggleBorder"
												Background="{TemplateBinding Background}"
												BorderBrush="{TemplateBinding BorderBrush}"
												BorderThickness="{TemplateBinding BorderThickness}"
												CornerRadius="2">
											<Grid>
												<Grid.ColumnDefinitions>
													<ColumnDefinition/>
													<ColumnDefinition Width="20"/>
												</Grid.ColumnDefinitions>
												<ContentPresenter Grid.Column="0"
																  Margin="8,0,0,0"
																  VerticalAlignment="Center"
																  Content="{Binding Path=SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
																  ContentTemplate="{Binding Path=SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType=ComboBox}}"/>
												<Path Grid.Column="1"
													  Fill="White"
													  Data="M0,0 L4,4 L8,0"
													  Width="8" Height="4"
													  Stretch="Fill"
													  HorizontalAlignment="Center"
													  VerticalAlignment="Center"/>
											</Grid>
										</Border>
										<ControlTemplate.Triggers>
											<Trigger Property="IsMouseOver" Value="True">
												<Setter Property="BorderBrush" Value="#FF25B6DE"/>
											</Trigger>
											<Trigger Property="IsPressed" Value="True">
												<Setter Property="Background">
													<Setter.Value>
														<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
															<GradientStop Color="#FF0A0A0A"/>
															<GradientStop Color="#FF151B20" Offset="1"/>
														</LinearGradientBrush>
													</Setter.Value>
												</Setter>
												<Setter Property="BorderBrush" Value="#FF1B8AA7"/>
											</Trigger>
										</ControlTemplate.Triggers>
									</ControlTemplate>
								</ToggleButton.Template>
							</ToggleButton>
							<Popup x:Name="PART_Popup"
								   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
								   AllowsTransparency="True"
								   Placement="Bottom"
								   PopupAnimation="Slide">
								<Border x:Name="DropDownBorder"
										Background="#FF1A2026"
										BorderThickness="1"
										BorderBrush="#FF20A1C3"
										Width="433"
										CornerRadius="2">
									<ScrollViewer MaxHeight="200">
										<ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
									</ScrollViewer>
								</Border>
							</Popup>
						</Grid>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<!-- Settings Group Style -->
		<Style x:Key="SettingsGroupStyle" TargetType="Border">
			<Setter Property="Background" Value="#FF1A2026"/>
			<Setter Property="BorderBrush" Value="#FF20A1C3"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="CornerRadius" Value="5"/>
			<Setter Property="Padding" Value="15"/>
			<Setter Property="Margin" Value="0,0,0,15"/>
		</Style>

		<!-- Settings Header Style -->
		<Style x:Key="SettingsHeaderStyle" TargetType="TextBlock">
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="FontSize" Value="16"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="Margin" Value="0,0,0,15"/>
		</Style>

		<!-- Settings Label Style -->
		<Style x:Key="SettingsLabelStyle" TargetType="TextBlock">
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="Margin" Value="0,0,15,0"/>
		</Style>

		<!-- Radio Button Style -->
		<Style x:Key="ModLoaderRadioButtonStyle" TargetType="{x:Type RadioButton}">
			<Setter Property="SnapsToDevicePixels" Value="true"/>
			<Setter Property="OverridesDefaultStyle" Value="true"/>
			<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type RadioButton}">
						<Border Background="{TemplateBinding Background}" BorderThickness="0">
							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto"/>
									<ColumnDefinition Width="*"/>
								</Grid.ColumnDefinitions>
								<Ellipse x:Name="RadioCircle" Width="16" Height="16" Margin="0,2,5,0" StrokeThickness="1" Stroke="#FF20A1C3" Fill="Transparent"/>
								<Ellipse x:Name="RadioMark" Width="8" Height="8" Margin="0,2,5,0" Fill="#FF20A1C3" Visibility="Collapsed"/>
								<ContentPresenter Grid.Column="1" Margin="0,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" RecognizesAccessKey="True"/>
							</Grid>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsChecked" Value="true">
								<Setter TargetName="RadioMark" Property="Visibility" Value="Visible"/>
							</Trigger>
							<Trigger Property="IsMouseOver" Value="true">
								<Setter TargetName="RadioCircle" Property="Stroke" Value="#FF25B6DE"/>
								<Setter TargetName="RadioMark" Property="Fill" Value="#FF25B6DE"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<!-- Save Button Style -->
		<Style x:Key="SaveButtonStyle" TargetType="Button">
			<Setter Property="BorderBrush" Value="#FF20A1C3"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="FontSize" Value="15"/>
			<Setter Property="Height" Value="40"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type Button}">
						<Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1">
							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto"/>
									<ColumnDefinition Width="*"/>
								</Grid.ColumnDefinitions>
								<iconPacks:PackIconMaterial Kind="ContentSave"
                                                          Width="16" Height="16"
                                                          Margin="15,0,10,0"
                                                          VerticalAlignment="Center"/>
								<ContentPresenter Grid.Column="1"
                                                Margin="0,0,15,0"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"/>
							</Grid>
						</Border>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
			<Setter Property="Background">
				<Setter.Value>
					<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
						<GradientStop Color="#FF111111"/>
						<GradientStop Color="#FF1A2026" Offset="1"/>
					</LinearGradientBrush>
				</Setter.Value>
			</Setter>
			<Style.Triggers>
				<Trigger Property="IsPressed" Value="True">
					<Setter Property="Background">
						<Setter.Value>
							<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
								<GradientStop Color="#FF0A0A0A"/>
								<GradientStop Color="#FF151B20" Offset="1"/>
							</LinearGradientBrush>
						</Setter.Value>
					</Setter>
					<Setter Property="BorderBrush" Value="#FF1B8AA7"/>
				</Trigger>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="BorderBrush" Value="#FF25B6DE"/>
				</Trigger>
			</Style.Triggers>
		</Style>
	</mah:MetroWindow.Resources>

	<Border Margin="15,10" Background="Transparent">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>

			<!-- Language Settings -->
			<Border Style="{StaticResource SettingsGroupStyle}" Grid.Row="0" Margin="0,0,0,10">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
					</Grid.RowDefinitions>

					<!-- Group Header -->
					<StackPanel Orientation="Horizontal" Margin="0,0,0,10">
						<iconPacks:PackIconMaterial Kind="Translate"
                                                      Width="20" Height="20"
                                                      Foreground="#FF20A1C3"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,10,0"/>
						<TextBlock Text="{DynamicResource Str.Settings.Language}"
                                 Foreground="White"
                                 FontSize="14"
                                 FontWeight="SemiBold"
                                 VerticalAlignment="Center"/>
					</StackPanel>

					<!-- Language Selection -->
					<ComboBox x:Name="LanguageComboBox"
                              Grid.Row="1"
                              Style="{StaticResource SettingsComboBoxStyle}">
						<ComboBoxItem Content="{DynamicResource Str.Language.English}"/>
						<ComboBoxItem Content="{DynamicResource Str.Language.Chinese}"/>
						<ComboBoxItem Content="{DynamicResource Str.Language.Russian}"/>
					</ComboBox>
				</Grid>
			</Border>

			<!-- Mod Loader Settings -->
			<Border Style="{StaticResource SettingsGroupStyle}" Grid.Row="1" Margin="0,0,0,10">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
					</Grid.RowDefinitions>

					<!-- Group Header -->
					<StackPanel Orientation="Horizontal" Margin="0,0,0,10">
						<iconPacks:PackIconMaterial Kind="FileReplace"
                                                      Width="20" Height="20"
                                                      Foreground="#FF20A1C3"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,10,0"/>
						<TextBlock Text="Mod Loader"
                                 Foreground="White"
                                 FontSize="14"
                                 FontWeight="SemiBold"
                                 VerticalAlignment="Center"/>
					</StackPanel>

					<!-- Mod Loader Selection -->
					<Grid Grid.Row="1" Margin="0,5,0,5">
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="Auto"/>
						</Grid.RowDefinitions>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>

						<!-- TSML Option -->
					<RadioButton x:Name="TsmlRadioButton" 
							   Grid.Row="0" Grid.Column="0"
							   Content="TSML (Recommended)" 
							   Style="{StaticResource ModLoaderRadioButtonStyle}"
							   FontWeight="SemiBold"
							   IsChecked="True"
							   GroupName="ModLoader"
							   Margin="0,5,0,5"/>

						<!-- SML Option -->
					<RadioButton x:Name="SmlRadioButton" 
							   Grid.Row="0" Grid.Column="1"
							   Content="Legacy SML" 
							   Style="{StaticResource ModLoaderRadioButtonStyle}" 
							   GroupName="ModLoader"
							   Margin="0,5,0,5"/>

						<!-- TSML Description -->
						<TextBlock Grid.Row="1" Grid.Column="0"
								   TextWrapping="Wrap"
								   Foreground="#CCCCCC"
								   FontSize="11"
								   Margin="20,0,5,0"
								   Text="Better security with anti-detection countermeasures, but may have occasional compatibility issues"/>

						<!-- SML Description -->
						<TextBlock Grid.Row="1" Grid.Column="1"
								   TextWrapping="Wrap"
								   Foreground="#CCCCCC"
								   FontSize="11"
								   Margin="20,0,5,0"
								   Text="Original loader with better compatibility across systems, but fewer security features"/>
					</Grid>
				</Grid>
			</Border>

			<!-- Game Path Settings -->
			<Border Style="{StaticResource SettingsGroupStyle}" Grid.Row="2" Margin="0,0,0,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Group Header -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10" Grid.Row="0">
                        <iconPacks:PackIconMaterial Kind="FolderCog"
                                              Width="20" Height="20"
                                              Foreground="#FF20A1C3"
                                              VerticalAlignment="Center"
                                              Margin="0,0,10,0"/>
                        <TextBlock Text="Game Installation"
                             Foreground="White"
                             FontSize="14"
                             FontWeight="SemiBold"
                             VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Game Path Display -->
                    <Border Grid.Row="1" 
                            Background="#1A2026" 
                            BorderBrush="#FF20A1C3" 
                            BorderThickness="1" 
                            CornerRadius="2"
                            Padding="8,6"
                            Margin="0,0,0,10">
                        <TextBlock x:Name="GamePathTextBlock"
                                 Text="{Binding GamePath, FallbackValue='No game path set'}"
                                 TextWrapping="Wrap"
                                 Foreground="White"
                                 FontSize="12"/>
                    </Border>

                    <!-- Change Location Button -->
                    <Button x:Name="BrowseGamePathButton"
                            Grid.Row="2"
                            Content="Browse"
                            Click="BrowseGamePathButton_Click"
                            Style="{StaticResource SaveButtonStyle}"
                            HorizontalAlignment="Stretch"
                            Margin="0,0,0,5"/>
                </Grid>
            </Border>

			<!-- Save Button -->
			<Button Grid.Row="3"
                    Content="{DynamicResource Str.Settings.Save}"
                    Style="{StaticResource SaveButtonStyle}"
                    Click="SaveButton_Click"
                    Margin="0,5,0,10"/>
		</Grid>
	</Border>
</mah:MetroWindow>