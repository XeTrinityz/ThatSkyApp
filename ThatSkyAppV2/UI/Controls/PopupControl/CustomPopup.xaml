<UserControl x:Class="ThatSkyAppV2.CustomPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="300">
    <Grid>
        <Border Background="#FF1A2026" CornerRadius="5" Padding="10,8,10,8">
            <!-- Changed padding to give more vertical space -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <!-- Changed from * to Auto -->
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <iconPacks:PackIconMaterial Kind="AlertCircle"
                                    Width="16"
                                    Height="16"
                                    Foreground="White" 
                                    Grid.Row="1" 
                                    VerticalAlignment="Top" />
                <!-- Changed to Top alignment -->

                <TextBlock x:Name="MessageText"
                   Grid.Row="1"
                   Text="Error Message"
                   Foreground="White"
                   TextWrapping="Wrap"
                   Margin="21,0,10,0"
                   FontSize="12"
                   VerticalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</UserControl>